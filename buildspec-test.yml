version: 0.2
phases:
  install:
    commands:
      - echo 'Nothing To Install Phase'
  pre_build:
    commands:
      - echo 'Nothing To Do In Pre-Build Phase'
  build:
    commands:
      - echo 'Nothing To Do In Build Phase'
  post_build:
    commands:
      - echo '-----------------------------------------------------------------------------------------'
      - kubectl version --client
      - echo '-----------------------------------------------------------------------------------------'
      - helm version
      - echo '-----------------------------------------------------------------------------------------'
      - python3 --version
      - echo '-----------------------------------------------------------------------------------------'
      - aws --version	  
      - echo '-----------------------------------------------------------------------------------------'
      - aws-iam-authenticator version
      - echo '----------------------------------------------------------------------------------------'
      - java -version
      - echo '----------------------------------------------------------------------------------------'
      - javac -version
      - echo '----------------------------------------------------------------------------------------'
      - mvn -version
      - echo '----------------------------------------------------------------------------------------'
